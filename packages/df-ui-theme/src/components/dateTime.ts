// import type { Theme } from "@mui/material"
import type { ComponentStyles } from "../types/Components"

export const getDateTime = (): ComponentStyles => {{

  return {
    MuiDateField: {
      defaultProps: {

      }
    }
  }
}}